apiVersion: v1
kind: Service
metadata:
  name: customer-portal-service
  labels:
    app: cargoflow-customer-portal
spec:
  type: ClusterIP
  selector:
    app: cargoflow-customer-portal
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
