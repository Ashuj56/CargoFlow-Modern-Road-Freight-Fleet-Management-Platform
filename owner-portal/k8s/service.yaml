apiVersion: v1
kind: Service
metadata:
  name: owner-portal-service
  labels:
    app: cargoflow-owner-portal
spec:
  type: ClusterIP
  selector:
    app: cargoflow-owner-portal
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
